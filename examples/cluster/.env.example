# Application
PORT=3100
NODE_ENV=development

# Redis (using in-memory stub for this example)
REDIS_HOST=localhost
REDIS_PORT=6379

# Cluster Configuration
CLUSTER_ENABLED=true
CLUSTER_MODE=auto
CLUSTER_WORKER_COUNT=4
