# Server Configuration
PORT=4000
HOST=0.0.0.0

# WebSocket Configuration
WS_HOST=0.0.0.0
WS_PORT=4000
WS_PUBLIC_HOST=localhost
# WS_URL=ws://localhost:4000/ws

# Environment
NODE_ENV=development

# Database Configuration (optional - set DB_ENABLED=true to enable)
DB_ENABLED=false
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE_NAME=hello_world

# Redis (required by framework validation)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Secret (required by framework validation)
JWT_SECRET=dev-secret-change-in-production
